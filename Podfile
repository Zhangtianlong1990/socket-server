# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'socket-server' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for socket-server
    pod 'CocoaAsyncSocket'
  target 'socket-serverTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'socket-serverUITests' do
    # Pods for testing
  end

end
